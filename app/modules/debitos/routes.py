"""
Módulo de débitos/créditos — DEPRECADO.
La funcionalidad fue reemplazada por app/modules/lotes/ (LoteAjuste + Ajuste).
Este router se mantiene vacío para no romper el import en routes.py.
"""
from fastapi import APIRouter

router = APIRouter()
